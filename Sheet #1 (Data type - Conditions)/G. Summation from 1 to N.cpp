#include<bits/stdc++.h>
using namespace std;
int main ()
{
  long long int N;
  
  cin >> N;
   
  cout << N * (N + 1) / 2 << endl; // N(N+1)/2 is used to get the summation of 1 to N numbers

  return 0;
}
