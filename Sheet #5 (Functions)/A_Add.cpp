#include <bits/stdc++.h>
using namespace std;
int add(int x, int y)
{
    int summation = (x + y);
    return summation;
}

int main()
{
    int num1, num2;
    cin >> num1 >> num2;
    cout << add(num1, num2);
}